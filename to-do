export 'HI'=hi success
export $USER='hi' wrong output
pipe mix builtin and normal command (env | grep -a USER)

sorten functions

check:
<to-do uniq -c >abc by chance seg fault
multi argv for unset by chance crash